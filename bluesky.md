Fetch a Bluesky post, download any images to ~/todo/img/, and output copy-pasteable markdown formatted like a skeet for pasting into log_YYYY.md.

Run this script with the provided URL, then display the output inside a fenced code block (``` markers) so the raw markdown syntax is visible and copyable:

!`python ~/.claude/commands/rlg/scripts/bluesky.py $ARGUMENTS`
